###############################################
# QUIC Client Environment Configuration (Example)
# Used by scripts/quic-client-batch.sh
###############################################

# ----------------------------------------------
# Target Server Configuration
# ----------------------------------------------
HOST=emes.bj
PORT=4447

# ----------------------------------------------
# Test Parameters
# Each test (T1, T2, T3) runs sequentially
# ----------------------------------------------
# Test 1 - Short upload
T1_N=1
T1_D=65536

# Test 2 - Medium upload
T2_N=5
T2_D=262144

# Test 3 - Long upload
T3_N=30
T3_D=262144

# ----------------------------------------------
# Paths and Directories
# (Override if you use a custom setup)
# ----------------------------------------------

# Base directory for logs
# Default for system-wide installs (root):
LOG_DIR=/var/log/quic-client

# For user-based installs (non-root alternative):
# LOG_DIR=$HOME/.quic-client/logs

# Path to quic-client binary
BIN_PATH=/usr/local/bin/quic-client

# Path to the batch script executed by cron
BATCH_SCRIPT=/usr/local/bin/quic-client-batch.sh

# ----------------------------------------------
# Logging Behavior
# ----------------------------------------------
# Log file naming convention:
#   cron-YYYYMMDD_HHMM.log
# Example:
#   /var/log/quic-client/cron-20251112_1030.log
#
# Each run creates a separate timestamped log file.

# ----------------------------------------------
# Cron Configuration (handled automatically)
# ----------------------------------------------
# The install script adds this entry to the root crontab:
#   0 */2 * * * /usr/local/bin/quic-client-batch.sh
#
# This means the QUIC client tests run every 2 hours.
# Logs are written in $LOG_DIR.

###############################################
# END OF FILE
###############################################

